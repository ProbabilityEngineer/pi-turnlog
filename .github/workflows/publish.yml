# Publishing is handled by release.yml, which builds platform binaries and tags only
# after all npm packages have been published successfully.
name: Legacy publish placeholder

on:
  workflow_dispatch:
    inputs:
      note:
        description: "No-op compatibility workflow"
        required: false
