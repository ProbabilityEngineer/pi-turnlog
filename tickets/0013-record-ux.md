# Record UX

Improve `/turnlog-record` for manual recording.

## Acceptance

- `/turnlog-record` with args passes through unchanged
- `/turnlog-record` with no args prompts for a summary
- empty prompt input cancels with a clear message
