# Structured command output

Have `pi-turnlog` preserve useful `turnlog` output and expose errors clearly.

## Acceptance

- stdout is shown to the user
- stderr is shown to the user
- non-zero exit codes fail the command
